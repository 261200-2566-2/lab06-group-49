public class fighter extends char_ implements Character{ //ประกาศคลาส fighter ที่ extends จากคลาส char_ และ implements จาก interface Character
    public fighter(String name, int level, double d, double s, double r) {
        super(name, level, d, s, r);
    }
}