public class carry extends char_ implements Character{ //ประกาศคลาส carry ที่ extends จากคลาส char_ และ implements จาก interface Character
    public carry(String name, int level, double d, double s, double r) {
        super(name, level, d, s, r);
    }
}